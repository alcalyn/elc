{% if isHost %}
    {% if slot.player is not null %}
        {% if slot.player.id == player.id %}
            {# host player me #}
            {% include 'ELCoreBundle:Slot/Option:slot-host-me.html.twig' %}
        {% else %}
            {# host player not me #}
            {% include 'ELCoreBundle:Slot/Option:slot-host.html.twig' %}
        {% endif %}
    {% else %}
        {% if slot.open %}
            {# host open #}
            {% include 'ELCoreBundle:Slot/Option:slot-host-open.html.twig' %}
        {% else %}
            {# host closed #}
            {% include 'ELCoreBundle:Slot/Option:slot-host-closed.html.twig' %}
        {% endif %}
    {% endif %}
{% else %}
    {% if slot.player is not null %}
        {# player #}
        {% include 'ELCoreBundle:Slot/Option:slot-player.html.twig' %}
    {% else %}
        {% if slot.open %}
            {# open #}
            {% include 'ELCoreBundle:Slot/Option:slot-open.html.twig' %}
        {% else %}
            {# closed #}
            {% include 'ELCoreBundle:Slot/Option:slot-closed.html.twig' %}
        {% endif %}
    {% endif %}
{% endif %}
